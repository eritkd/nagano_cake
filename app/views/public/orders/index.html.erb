
  <div class="container px-5 px-sm-0 mt-5">
    <div class="row">
        
      
        <div class="p-1 w-25 bg-light text-dark text-center mb-5"><h4>　注文履歴　</h4></div>
      </div>
      
      <div class="row justify-content-center">
        <div class="col-md-12">
<table class="table table-inverse" border=1 width=auto height=auto cellpadding=10>
  <thead class="thead-light">
    <tr>
      <th scope="col">注文日</th>
      <th scope="col">配送先</th>
      <th scope="col">注文商品</th>
    　<th scope="col">支払金額</th>
    　<th scope="col">注文詳細</th>
    </tr>
  </thead>
  <tbody>
    <tr>
        <% @orders.each do |f| %>
        <td>
       <%= f.created_at.strftime('%Y/%m/%d') %>
      </td>
      <td>
       <%= f.postal_code %><%= f.address %><%= f.name %>
      </td>
      <%= f.postal_code %>
      <td>
      <%= cart_item.name %>
      </td>
      <td>
      <!--<*%= link_to "表示する", public_order_path(@order.id), class: "btn btn-success" %>-->
      </td>
      <td width="150">
       <!--<*%= link_to "sakujo", public_order_path(order.id), method: :delete, class: "btn btn-danger" %>-->
      </td>
  </tr>
    <% end %>
    
  </tbody>
</table>


 
 
</div>
</div>


